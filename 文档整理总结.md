# 📚 文档整理总结

## 🗂️ 文档清理结果

### ✅ 保留的核心文档

#### 主要文档
- **README.md** - 项目主页，包含快速开始和基本API介绍
- **Andorralee蜜罐管理系统使用指南.md** - 完整的使用指南和API文档

#### 部署文档
- **银河麒麟Docker部署指南.md** - 银河麒麟系统专用部署指南
- **deploy-kylin.sh** - Linux自动部署脚本
- **deploy-kylin.ps1** - Windows远程部署脚本

#### 测试和调试工具
- **test-container-api.ps1** - PowerShell API测试脚本
- **test-kylin-deployment.sh** - 银河麒麟部署测试脚本
- **fix-container-issues.sh** - 问题诊断和修复脚本
- **static/debug-container.html** - Web调试界面

#### 专业文档 (docs目录)
- **docs/quick_start_guide.md** - 快速开始指南
- **docs/powershell_operations_guide.md** - PowerShell操作指南
- **docs/cowrie_implementation_guide.md** - Cowrie实现指南
- **docs/complete_system_features.md** - 系统功能完整说明
- **docs/headling_implementation_summary.md** - Headling实现总结

### ❌ 已删除的重复/无用文档

#### 重复文档
- ~~README-银河麒麟.md~~ (内容已合并到银河麒麟Docker部署指南.md)
- ~~Docker部署指南.md~~ (内容已合并到主要文档)
- ~~项目结构说明.md~~ (内容已合并到README.md)

#### 开发临时文档
- ~~开发文档/powershell操作命令.md~~ (已整合到正式文档)
- ~~开发文档/curl操作命令.md~~ (已整合到API文档)
- ~~开发文档/ai聊天.md~~ (开发过程记录，无需保留)
- ~~开发文档/cursor对话.txt~~ (开发工具记录)
- ~~开发文档/数据库表.md~~ (信息已过时)

#### 过时的部署脚本
- ~~docker-deploy.ps1~~ (被deploy-kylin.ps1替代)
- ~~docker-deploy.sh~~ (被deploy-kylin.sh替代)

#### 测试和临时文件
- ~~test_dameng_connection.go~~ (临时测试文件)
- ~~examples/~~ (示例代码已过时)

## 📖 文档使用指南

### 🎯 新用户推荐阅读顺序

1. **README.md** - 了解项目概况和快速开始
2. **docs/quick_start_guide.md** - 详细的新手入门指南
3. **Andorralee蜜罐管理系统使用指南.md** - 完整的API文档和使用示例

### 🐧 银河麒麟系统用户

1. **银河麒麟Docker部署指南.md** - 专用部署指南
2. **deploy-kylin.sh** - 自动部署脚本
3. **test-kylin-deployment.sh** - 部署验证脚本

### 💻 Windows用户

1. **docs/powershell_operations_guide.md** - PowerShell操作指南
2. **deploy-kylin.ps1** - 远程部署脚本
3. **test-container-api.ps1** - API测试脚本

### 🔧 开发者

1. **docs/complete_system_features.md** - 系统功能完整说明
2. **docs/cowrie_implementation_guide.md** - Cowrie实现指南
3. **docs/headling_implementation_summary.md** - Headling实现总结

## 🔧 调试和故障排除

### Web调试界面
访问: `http://localhost:8081/static/debug-container.html`

功能包括：
- 容器实例管理
- 状态同步
- 详细调试信息
- API测试功能

### 命令行工具

#### PowerShell测试 (Windows)
```powershell
# 完整API测试
.\test-container-api.ps1 -Verbose

# 自定义服务器测试
.\test-container-api.ps1 -BaseUrl "http://192.168.1.100:8081"
```

#### Linux问题修复
```bash
# 交互式问题诊断
chmod +x fix-container-issues.sh
./fix-container-issues.sh

# 自动修复所有问题
./fix-container-issues.sh all
```

## 📋 API快速参考

### 基础URL
```
http://localhost:8081/api/v1
```

### 核心接口

#### 健康检查
```bash
GET /health                    # 简单健康检查
GET /api/v1/health            # 详细健康检查
```

#### 容器管理
```bash
POST /api/v1/container-instances              # 创建容器实例
GET  /api/v1/container-instances              # 获取所有容器
POST /api/v1/container-instances/:id/start    # 启动容器
POST /api/v1/container-instances/:id/stop     # 停止容器
POST /api/v1/container-instances/sync         # 同步状态
GET  /api/v1/container-instances/:id/debug    # 调试信息
```

#### Docker管理
```bash
GET  /api/v1/docker/images     # 获取镜像列表
POST /api/v1/docker/pull       # 拉取镜像
GET  /api/v1/docker/containers # 获取容器列表
```

#### 日志分析
```bash
POST /api/v1/headling/pull-logs    # 拉取认证日志
GET  /api/v1/headling/statistics   # 获取统计信息
POST /api/v1/cowrie/pull-logs      # 拉取Cowrie日志
GET  /api/v1/cowrie/top-commands   # 获取常用命令
```

## 🎯 使用场景示例

### 场景1: 快速部署SSH蜜罐
```bash
# 1. 创建SSH蜜罐实例
curl -X POST http://localhost:8081/api/v1/container-instances \
  -H "Content-Type: application/json" \
  -d '{
    "name": "SSH蜜罐1",
    "honeypot_name": "ssh-honeypot-1",
    "image_name": "ubuntu:20.04",
    "protocol": "ssh",
    "port_mappings": {"22": "2222"},
    "auto_start": true
  }'

# 2. 检查容器状态
curl http://localhost:8081/api/v1/container-instances

# 3. 拉取日志
curl -X POST http://localhost:8081/api/v1/headling/pull-logs \
  -d '{"container_id": "your_container_id"}'
```

### 场景2: 批量管理容器 (PowerShell)
```powershell
# 获取所有容器状态
$containers = (Invoke-WebRequest -Uri "http://localhost:8081/api/v1/container-instances").Content | ConvertFrom-Json

# 启动所有停止的容器
foreach ($container in $containers.data) {
    if ($container.status -eq "stopped") {
        Invoke-WebRequest -Method POST -Uri "http://localhost:8081/api/v1/container-instances/$($container.id)/start"
    }
}

# 同步所有容器状态
Invoke-WebRequest -Method POST -Uri "http://localhost:8081/api/v1/container-instances/sync"
```

### 场景3: 攻击分析报告
```bash
# 生成攻击分析报告
echo "=== 攻击分析报告 ===" > attack_report.txt
echo "生成时间: $(date)" >> attack_report.txt
echo "" >> attack_report.txt

# Headling统计
echo "=== Headling认证攻击统计 ===" >> attack_report.txt
curl -s http://localhost:8081/api/v1/headling/statistics >> attack_report.txt
echo "" >> attack_report.txt

# 顶级攻击者
echo "=== 顶级攻击者 ===" >> attack_report.txt
curl -s http://localhost:8081/api/v1/headling/top-attackers?limit=10 >> attack_report.txt
echo "" >> attack_report.txt

# Cowrie命令统计
echo "=== Cowrie命令统计 ===" >> attack_report.txt
curl -s http://localhost:8081/api/v1/cowrie/top-commands?limit=20 >> attack_report.txt

echo "报告已生成: attack_report.txt"
```

## 📞 技术支持

### 获取帮助的优先级

1. **查看文档** - 首先查阅相关文档
2. **使用调试工具** - 利用Web界面和命令行工具
3. **查看日志** - 检查应用和容器日志
4. **联系支持** - 提供详细的错误信息

### 联系方式
- 📧 邮箱: support@andorralee.com
- 🐛 问题反馈: GitHub Issues
- 💬 技术交流: QQ群 123456789

---

**文档整理完成时间**: $(date)  
**整理版本**: v1.3.0  
**文档总数**: 保留12个核心文档，删除15个重复/无用文档
